const choices = ['Rock', 'Paper', 'Scissors'];

function getComputerChoice() {
  let randomChoice = choices[Math.floor(Math.random() * choices.length)];
  return randomChoice;
}
const computerSelection = getComputerChoice();

let playerSelection = 

let draw = {
  switch (if playerSelection && computerSelection == 'Rock' || 'Paper' || 'Scissors') {
  case 'Rock':
    return "Rock does not beat Rock, draw!";
    break;
  case 'Scissors':
    return "Scissors does not beat Scissors, draw!";
    break;
  case 'Paper':
    return "Paper does not beat Paper, draw!";
    break;
  }
};  

function winner(playerSelection, computerSelection) {
  if playerSelection == true && computerSelection == false {
    return "Player "
    else 
  }
};




function (playerSelection, computerSelection) {

 
  if (playerSelection == 'Rock' && computerSelection == 'Scissors') {
    return "Rock beats Scissors,  wins!"
  }
  if (playerSelection == 'Rock' && computerSelection == 'Scissors') {
    return "Rock beats Scissors,  wins!"
  }
  if (playerSelection == 'Rock' && computerSelection == 'Scissors') {
    return "Rock beats Scissors,  wins!"
  }
 };


};
